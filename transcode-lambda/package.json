{
  "name": "alive-transcode-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda that re-encodes uploaded ALIVE content videos to H.264 Main Profile / Level 4.1 so they hardware-decode reliably on budget Android TV SoCs (Realtek, Amlogic, Allwinner) that reject High Profile / Level 5.0 sources.",
  "main": "index.mjs",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@ffprobe-installer/ffprobe": "^2.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
